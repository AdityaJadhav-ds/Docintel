# Core dependencies
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
python-dotenv==1.0.1

# Supabase
supabase==2.4.3

# OCR — PaddleOCR ONLY (Tesseract removed)
paddleocr==2.7.3
paddlepaddle==2.6.1

# PDF rendering — PyMuPDF ONLY (no fallback engines)
pymupdf>=1.23.0

# Image processing
opencv-python-headless==4.9.0.80
Pillow==10.3.0
numpy==1.26.4

# Matching (used by other routes)
rapidfuzz==3.9.3

# Testing
pytest==8.2.0
pytest-asyncio==0.23.7
httpx==0.27.0
